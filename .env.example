# Django Settings
DJANGO_SETTINGS_MODULE=speakup.settings.development
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Bitrix CRM Integration
BITRIX_WEBHOOK_URL=https://your-domain.bitrix24.com/rest/1/your-webhook-code/

# Email Settings (Fallback for Bitrix)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@speakup.com
CONTACT_EMAIL=admin@speakup.com

# Email Settings for Production (SMTP)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password

# Database (for production with PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/speakup_db
